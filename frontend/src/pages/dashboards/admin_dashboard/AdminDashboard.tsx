import { Outlet } from "react-router-dom";

const AdminDashboard = () => {
    return (
        <div>
            admin profileeeeee
            <Outlet />
        </div>
    )
}

export default AdminDashboard;